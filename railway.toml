[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node fix-schema.js && node server-v2.js"
healthcheckPath = "/v1/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "slopshop"
