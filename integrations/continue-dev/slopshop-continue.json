{
  "name": "slopshop",
  "displayName": "Slopshop — AI Agent Backend",
  "description": "1,273+ real compute handlers, free persistent memory, MCP server",
  "type": "mcp",
  "config": {
    "command": "npx",
    "args": ["slopshop", "mcp", "serve"],
    "env": {
      "SLOPSHOP_KEY": "${SLOPSHOP_KEY}"
    }
  }
}
