version: '3.8'
services:
  slopshop:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    volumes:
      - slopshop-data:/app/.data
    restart: unless-stopped

volumes:
  slopshop-data:
